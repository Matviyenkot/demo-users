# demo-users

# Requirements:
- Java 21 +
- Docker

# Run the project:
In console from the root folder run command "docker compose up --build"

# Run the tests:
Add -Duser.timezone=Europe/Kyiv to VM options